#include <stdio.h>
#include <stdlib.h>

int main() {
    int vetor[10];
    int i;

    for(i=0; i<10; i++){
        printf("Digite o valor do vetor[%d]: ", i+1);
        scanf("%d", &vetor[i]);
    }
    for(i=0; i<10; i++){
        printf("Vetor[%d] = %d\n", i+1, vetor[i]);
    }
    return 0;
}
